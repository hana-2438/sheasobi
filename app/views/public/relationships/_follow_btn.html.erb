    <% if current_member.following?(member) %>
      <%= link_to "フォロー中",  member_relationships_path(member), method: :delete, remote: true, class: "btn btn-salmon rounded-pill" %>
    <% else %>
      <%= link_to "フォローする", member_relationships_path(member), method: :post, remote: true, class: "btn btn-green rounded-pill" %>
    <% end %>

